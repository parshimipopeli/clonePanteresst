{# Partial for form
 #}


{{ form_start(form) }}


    {{ form_widget(form)}}
    <input type="submit" value="{{ submitButtonText }}" class="btn btn-primary btn-block" formnovalidate >  {#formnovalidate pour empecher la validaton formulaire html #}


{{ form_end(form) }}
